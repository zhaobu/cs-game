关于*.proto协议的约定
    1）类型名（结构类型和枚举类型）不能为全大写
    2）不能在结构中嵌套声明结构和枚举类型
    3）以bytes方式嵌套结构时，bytes字段名要命名成：xxxValue的形式，并且它的所在结构中要有一个字段来存储被嵌套结构的名字，这个字段要命名成：xxxName的形式.
    4) 将bytes当成，byte字数数组时，字段的要命名成：xxxArray的形式
    5）同一个结构中不能同时定义多个repeated基本类型字段, 这个情况要将基本类单独包一下.
    6）定义枚举时，0不做为实际有效的枚举值